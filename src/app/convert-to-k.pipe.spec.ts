import { ConvertToKPipe } from './convert-to-k.pipe';

describe('ConvertToKPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToKPipe();
    expect(pipe).toBeTruthy();
  });
});
